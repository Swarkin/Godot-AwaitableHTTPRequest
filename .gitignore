# I don't see a need for a gitignore as downloading a .zip only contains the .gd script, but I can add one just for the sake of adding it

# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json
